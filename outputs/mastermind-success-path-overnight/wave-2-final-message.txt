Wave 2 critical repair is implemented in the uncommitted tree at HEAD f93c0f691347b1add6663adedc244153cd33646d.

Catalog revocation now fails the assigned-Learning resolver closed through a service-only, append-only, terminal audited transition. Rebuilds persist a server-derived exact frozen-authority diff and require exact diff plus canonical hash confirmation. Publication hashing binds complete final stored catalog/item/media/QA authority, and assignments freeze and revalidate those receipts. The native verifier covers every requested denied state and private/Vault sentinel plus a verifier mutation control.

Verified locally: Wave 2 static 131 checks, TypeScript, focused ESLint, production build, Replay Vault 74/74 baseline, all Replay Vault baseline mutation controls, source scans, and `git diff --check`. Repository-wide lint retains pre-existing failures outside repair scope.

Blocked locally: focused PostgreSQL, full chronological PostgreSQL, and full npm verify cannot pass because this managed sandbox prevents PostgreSQL bootstrap shared memory. Unchanged mounted browser/listener aggregate children are also sandbox-blocked. No database behavior acceptance is claimed.

No commit, push, deploy, production migration, real data seed, SaaS mutation, member exposure, or Wave 3 work occurred. Production remains blocked and untouched. Parent must run both PostgreSQL 16 gates and the full aggregate on the exact uncommitted tree.
